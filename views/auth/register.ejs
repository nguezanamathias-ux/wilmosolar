<%- include('../partials/header') %>

<div class="container auth-layout">
    <div class="auth-shell">
        <div class="auth-aside">
            <span class="auth-badge">Demarrage rapide</span>
            <h2>Créez votre compte Wilmosolar</h2>
            <p>Rejoignez une plateforme energetique claire, fiable et axee sur la performance.</p>
            <div class="auth-highlights">
                <div class="auth-highlight">Activation simple en quelques minutes</div>
                <div class="auth-highlight">Suivi detaille des gains et operations</div>
                <div class="auth-highlight">Equipe support reactive en cas de besoin</div>
            </div>
            <div class="auth-footnote">
                Vos informations restent strictement confidentielles.
            </div>
        </div>
        <div class="auth-card auth-container">
            <h2 class="form-title">Inscription</h2>
            <% if (error) { %>
                <div class="alert alert-error"><%= error %></div>
            <% } %>
            <form action="/register" method="POST" class="auth-form">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="form-group">
                    <label class="form-label">Numero de Telephone</label>
                    <input type="tel" class="form-input" name="phone" placeholder="+237 6XX XXX XXX" value="<%= body.phone || '' %>" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Nom complet (optionnel)</label>
                    <input type="text" class="form-input" name="full_name" placeholder="Votre nom" value="<%= body.full_name || '' %>">
                </div>
                <div class="form-group">
                    <label class="form-label">Mot de Passe</label>
                    <input type="password" class="form-input" name="password" placeholder="Minimum 8 caracteres" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Confirmation</label>
                    <input type="password" class="form-input" name="confirm_password" placeholder="Retaper le mot de passe" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Code d'Invitation (Optionnel)</label>
                    <input type="text" class="form-input" name="invitation_code" placeholder="Code parrain" value="<%= ref || body.invitation_code || '' %>">
                </div>
                <button type="submit" class="btn btn-primary">S'inscrire</button>
                <div class="form-helper">Votre compte sera active apres validation.</div>
            </form>
            <div class="link-text">
                Vous avez deja un compte  <a href="/login">Se connecter</a>
            </div>
        </div>
    </div>
</div>

<%- include('../partials/footer') %>
